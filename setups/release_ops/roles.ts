import type { RoleInput } from "paperzod";

export const roles = [
  {
    id: "coordinator",
    name: "Coordinator",
    purpose: "Coordinate the release workflow and keep the shared release operations doctrine coherent."
  }
] satisfies RoleInput[];
